# TG-AI智控王 開發規範

## 語言與回應
- 始終使用**繁體中文**回覆
- 代碼註釋可用簡體中文或英文

## 技術棧
- **前端**: Angular 21 + TypeScript (Standalone Components)
- **後端**: Python 3.11+ / aiohttp + Pyrofork (Telegram API)
- **桌面**: Electron 28
- **資料庫**: SQLite (三數據庫架構)
- **通訊**: Electron IPC (stdin/stdout JSON)

---

## 🔴 數據庫架構規範（嚴格遵守）

### 三數據庫設計

| 數據庫 | 路徑 | 用途 | 管理模塊 |
|--------|------|------|----------|
| `tgmatrix.db` | `backend/data/` | 帳號管理 | `database.py` |
| `tgai_server.db` | `backend/data/` | 業務數據 | `database.py` |
| `search_history.db` | `backend/data/` | 搜索歷史 | `search_history_service.py` |

### 🚫 嚴禁事項
1. **禁止在其他文件創建數據庫連接** - 必須通過指定模塊
2. **禁止硬編碼數據庫路徑** - 必須從 `config.py` 獲取
3. **禁止重複定義表結構** - 表定義唯一來源見下方
4. **禁止跨數據庫查詢** - 每個表只存在於一個數據庫

### 🆕 合法連接模塊清單（2026-07 系統排查後補充）

系統排查發現 `backend/` 下約 109 個檔案直接開 sqlite 連接，繞過上方指定模塊。
逐一排查後確認：其中 `core/db_utils.py`、`db_connection_pool.py` 雖然技術上
也直接呼叫 `sqlite3.connect()`/`aiosqlite.connect()`，但屬於**刻意設計的
集中式工具**（分別提供同步輔助查詢與異步連接池），已被 11+ 處正確採用，
廢棄它們的成本遠高於承認現狀。故正式列入合法連接模塊：

| 模塊 | 定位 | 適用場景 |
|------|------|----------|
| `database.py` | 主庫（`tgmatrix.db`/`tgai_server.db`）唯一 async 連接來源 | 業務邏輯主流程 |
| `search_history_service.py` | `search_history.db` 唯一連接來源 | 搜索歷史相關 |
| `core/tenant_database.py` | 多租戶 SaaS 模式下每租戶獨立 db 的連接來源 | 租戶隔離場景 |
| `core/db_utils.py` | 同步輔助查詢的唯一合法來源（`get_connection()`/`create_connection()`） | 非 async 上下文的簡單讀寫 |
| `db_connection_pool.py` | 高頻查詢的專用異步連接池 | 效能敏感的重複查詢場景 |

其餘直接 `sqlite3.connect()`/硬編碼路徑的檔案（`admin/*`、`auth/*`、
`core/payment_service.py` 等約 70 個 D 類高風險檔案）屬於待修復的技術債，
正在依風險分批遷移到上方合法模塊，詳見對話記錄中的完整盤點報告，
**修復前必須先確認該檔案是否仍被其他測試/生產代碼引用**（曾發現盤點
報告誤判「零引用」的案例，實際仍被測試依賴）。

### 表歸屬映射（唯一來源）

#### tgmatrix.db（帳號專用）
| 表名 | 定義位置 | 說明 |
|------|----------|------|
| `accounts` | `database.py:_init_db()` | Telegram 帳號 |

#### tgai_server.db（業務數據）
| 表名 | 定義位置 | 說明 |
|------|----------|------|
| `monitored_groups` | `database.py:_init_db()` | 監控群組 |
| `keyword_sets` | `database.py:_init_db()` | 關鍵詞集 |
| `trigger_rules` | `database.py:_init_db()` | 觸發規則 |
| `chat_templates` | `database.py:_init_db()` | 聊天模板 |
| `extracted_members` | `database.py:_init_db()` | 提取成員 |
| `unified_contacts` | `database.py:_init_db()` | 統一聯繫人 |
| `message_queue` | `database.py:_init_db()` | 消息隊列 |
| `chat_history` | `database.py:_init_db()` | 聊天記錄 |
| `leads` | `database.py:_init_db()` | 潛在客戶 |
| `collected_users` | `database.py:_init_db()` | 收集用戶 |
| `logs` | `database.py:_init_db()` | 系統日誌 |
| `settings` | `database.py:_init_db()` | 系統設置 |

#### search_history.db（搜索專用）
| 表名 | 定義位置 | 說明 |
|------|----------|------|
| `discovered_resources` | `search_history_service.py` | 發現的資源 |
| `search_history` | `search_history_service.py` | 搜索歷史 |
| `search_result_items` | `search_history_service.py` | 搜索結果 |

### 數據流向圖
```
[搜索發現] ──► search_history.db:discovered_resources
                         │
                    [加入群組]
                         ▼
              tgai_server.db:monitored_groups
                         │
                    [成員提取]
                         ▼
              tgai_server.db:extracted_members
                         │
                    [同步整合]
                         ▼
              tgai_server.db:unified_contacts ◄── leads
                         │
                    [批量發送]
                         ▼
              tgai_server.db:message_queue
                         │
                         ▼
              tgai_server.db:chat_history
```

### 數據庫訪問規範

```python
# ✅ 正確：通過 database.py 訪問
from database import db
await db.fetch_all("SELECT * FROM monitored_groups")

# ✅ 正確：通過專用服務訪問
from search_history_service import get_search_history_service
service = get_search_history_service()
resources = service.get_discovered_resources()

# ❌ 錯誤：直接創建連接
conn = sqlite3.connect("backend/data/tgai_server.db")

# ❌ 錯誤：硬編碼路徑
db_path = "D:/tgkz2026/backend/data/tgai_server.db"
```

### 表結構修改流程
1. **僅修改** `database.py` 的 `_init_db()` 或 `_migrate_db()`
2. 添加新欄位使用 `ALTER TABLE ADD COLUMN`
3. 修改後測試現有數據遷移
4. 更新本文檔的表歸屬映射

---

## 關鍵文件規範

### 禁止隨意修改的文件

| 文件 | 職責 | 修改風險 |
|------|------|----------|
| `backend/config.py` | 所有配置項來源 | 🔴 極高 |
| `backend/database.py` | 數據庫連接和表定義 | 🔴 極高 |
| `backend/search_history_service.py` | 搜索歷史數據庫 | 🔴 極高 |
| `electron.js` | IPC 通訊和進程管理 | 🔴 極高 |

### 修改這些文件前必須
1. 理解現有邏輯
2. 確認修改不會破壞現有功能
3. 測試所有相關功能
4. 在 commit message 中說明原因

---

## 核心表關係圖

```
tgmatrix.db                    tgai_server.db
┌─────────────┐               ┌─────────────────┐
│  accounts   │──────────────►│ monitored_groups │
│  (3 records)│               │   (關聯帳號)      │
└─────────────┘               └────────┬────────┘
                                       │
                              ┌────────▼────────┐
                              │ keyword_sets    │
                              │ trigger_rules   │
                              │ chat_templates  │
                              └────────┬────────┘
                                       │
                              ┌────────▼────────┐
                              │extracted_members│
                              │   (14 records)  │
                              └────────┬────────┘
                                       │
                              ┌────────▼────────┐
                              │unified_contacts │
                              │(同步整合視圖)    │
                              └─────────────────┘

search_history.db
┌───────────────────┐
│discovered_resources│
│   (367 records)   │
└───────────────────┘
```

---

## 代碼風格
- Python: `snake_case`，類名 `PascalCase`
- TypeScript: `camelCase`，類名/組件 `PascalCase`
- 資料庫欄位: `snake_case`

## API 規範
後端所有響應必須遵循統一格式：
```python
{"success": True/False, "data": ..., "error": "錯誤訊息"}
```

## 資料庫操作注意事項
- **N+1 問題**: 列表查詢必須使用 JOIN 或批量獲取
- **事務**: 多表更新必須使用事務包裹
- **索引**: 新增查詢條件欄位時考慮是否需要索引
- **分頁**: 返回大量數據時必須支持 `limit/offset`
- **NOT NULL**: 謹慎使用，避免 INSERT 失敗
- **觸發器**: 禁止創建，曾導致嚴重問題

## Telegram/Pyrogram 特別注意
- **FloodWait**: 所有 API 調用必須通過 `safe_telegram_call()` 包裹
- **Session 文件**: 操作前檢查鎖定狀態，使用 `try-finally` 釋放
- **API_ID/API_HASH**: 禁止硬編碼，必須從 config 讀取
- **帳號狀態**: 操作前驗證 `client.is_connected()`

## 前端 UI 規範
- 所有操作需提供 Loading 狀態反饋
- 列表為空時顯示空狀態提示
- 錯誤使用 Toast 通知用戶
- `@for` 循環必須使用唯一的 `track` 表達式

## 錯誤處理
- 後端: 使用 `handle_error()` 統一處理
- 前端: 捕獲所有 API 錯誤，不讓錯誤靜默失敗
- 日誌: 關鍵操作記錄 INFO，異常記錄 ERROR + 堆棧

## 安全紅線
- 不在前端存儲敏感憑證
- 所有用戶輸入必須驗證
- 帳號操作需驗證用戶權限和配額

## 已知問題區域（修改時特別小心）
- `telegram_client.py`: Session 管理、連線狀態
- `message_queue.py`: 並發控制、隊列狀態
- `search-discovery.component.ts`: 資源列表渲染
- `database.py`: 遷移兼容性、表定義
- `unified_contacts.py`: 跨表同步邏輯

---

## 🔴 認證與會話管理規範（嚴格遵守）

### Token 驗證規則
1. **永遠不要只靠 `token.length` 判斷認證狀態** — 必須解析 JWT payload 檢查 `exp` 過期時間
2. **前端判斷 Token 有效性的標準方法**：
   ```typescript
   // ✅ 正確：解析 JWT 檢查過期
   function isTokenAlive(token: string | null): boolean {
     if (!token || token.length < 20) return false;
     const parts = token.split('.');
     if (parts.length !== 3) return false;
     try {
       let base64 = parts[1].replace(/-/g, '+').replace(/_/g, '/');
       while (base64.length % 4) base64 += '=';
       const payload = JSON.parse(atob(base64));
       return !(payload.exp && Date.now() >= payload.exp * 1000);
     } catch { return false; }
   }
   
   // ❌ 錯誤：只檢查長度
   if (token && token.length > 10) { /* 已認證 */ }
   ```
3. **Guard 認證邏輯三步驟**：
   - Access Token 有效 → 放行
   - Access Token 過期 + Refresh Token 有效 → 放行（背景刷新）
   - 兩個都過期 → 清除 localStorage + 重定向登入頁
4. **`restoreSession()` 恢復時**：必須檢查 Token 過期，過期的 Token 不要設入 Signal
5. **App 啟動後必須調用 `fetchCurrentUser()`** 向後端驗證 Token，如果返回 null/401，清除會話並跳轉登入頁

### 🚫 認證嚴禁事項
1. **禁止在 localStorage 存放密碼或密鑰** — 只存 JWT Token
2. **禁止硬編碼 JWT Secret** — 必須從環境變量 `JWT_SECRET` 讀取
3. **禁止在 AuthGuard 中只檢查 token.length** — 必須用 `isTokenAlive()` 解析過期
4. **禁止在 401 處理中添加過長的寬限期** — 最多 5 秒
5. **禁止繞過認證的後門代碼** — Electron 模式例外需要同時滿足 `apiMode === 'ipc' && isElectron`

### 多租戶（SaaS）數據隔離規則
1. **帳號查詢必須帶 `owner_user_id` 過濾** — SaaS 模式下每個用戶只能看到自己的帳號
2. **添加帳號時必須設置 `owner_user_id`** — 綁定到當前登入用戶
3. **兼容歷史數據** — 查詢時包含 `owner_user_id IS NULL OR owner_user_id = 'local_user'`
4. **測試多租戶時** — 使用不同用戶登入，確認數據隔離正確

---

## 🔴 數據庫持久化防護規範

### 防止數據丟失的檢查清單
1. **部署後檢查數據庫路徑** — `docker exec` 進容器確認 `/app/data/tgmatrix.db` 存在
2. **Docker Volume 必須掛載** — `docker-compose.yml` 中 `./data:/app/data` 不可刪除
3. **數據庫操作必須用事務** — 多步驟更新使用 `BEGIN/COMMIT`
4. **WAL 模式保持開啟** — `PRAGMA journal_mode=WAL` 防止並發寫入衝突
5. **重啟前檢查 WAL 文件** — 確保 `-wal` 文件已 checkpoint

### 數據庫路徑解析優先級
| 環境 | 路徑 | 注意事項 |
|------|------|----------|
| 開發 | `backend/data/tgmatrix.db` | 本地直接訪問 |
| Docker | `/app/data/tgmatrix.db` | 必須掛載 Volume |
| Electron | `{userData}/backend-data/tgmatrix.db` | 由 Electron 傳遞路徑 |

---

## 🔴 登錄 Token 與掃碼後端統一規範（嚴格遵守）

### 原則：只應有一個「生成並驗證 login token」的後端

掃碼登錄流程：**網頁/客戶端** 請求 `POST /api/v1/auth/login-token` 在**該後端**生成 token 並寫入**該後端**的數據庫 → 用戶在 Telegram 掃碼 → **Bot** 調用 `INTERNAL_API_URL` 對應的後端查詢/確認 token。若 Bot 調用的後端與生成 token 的後端**不是同一個**（或不是同一份數據庫），會出現「Token 不存在」登錄失敗。

### 三端與後端對齊規則

| 端 | 建議行為 | 說明 |
|----|----------|------|
| **網頁端**（如 https://tgw.usdt2026.cc） | 不設置 api_server，用同源 | 登錄頁請求同源 API，token 寫在該服務器 DB；Bot 必須與該 API 同機同庫。 |
| **本地開發**（npm run start + 掃碼） | 若需掃碼成功，將「使用服務器登錄」設為**生產地址**（如 https://tgw.usdt2026.cc）並保存 | 否則 token 在本地 8000 的 DB，服務器上的 Bot 查不到。 |
| **安裝版（Electron）** | 同上，掃碼登錄時 api_server 應指向**與 Bot 同一後端**（通常為生產地址） | 與管理後台、網頁版共用同一套數據與 token 庫。 |

### 後端與 Bot 部署要求

1. **只部署一個對外提供登錄頁 + login-token API 的後端實例**（同一 `DATABASE_PATH`），避免多實例、多庫導致 token 不一致。
2. **Telegram Bot 的 `INTERNAL_API_URL`** 必須指向**該同一後端**（或同機同庫的 API）：
   - 生產：Bot 與 API 同機時設 `INTERNAL_API_URL=http://127.0.0.1:8000`（或該 API 的內網地址），且該 8000 進程即對外提供登錄的後端。
   - 禁止：Bot 指向 A 機、登錄頁請求 B 機，或 Bot 與 API 使用不同 `DATABASE_PATH`。
3. **環境變量**：該後端須配置 `TELEGRAM_BOT_USERNAME`、`TELEGRAM_BOT_TOKEN`（與 Bot 一致），以便生成正確的二維碼連結。

### 代碼與配置要點

- 前端：登錄/掃碼時 API 基地址由 `localStorage['api_server']` 或同源決定（`getApiBaseForFetch()`）。
- 後端：Bot 在 `backend/telegram/bot_handler.py` 使用 `os.environ.get('INTERNAL_API_URL', 'http://localhost:8000')`；部署時必須在運行 Bot 的環境中設置 `INTERNAL_API_URL` 為上述「唯一後端」的地址。
- 新增或修改登錄/掃碼相關邏輯時，不得引入「多個後端生成 token、Bot 只問其中一個」的配置，必須保持「一個後端 + 一個 DB + Bot 指向該後端」。

---

## 測試建議
修改功能後，簡述如何手動測試：
1. 正常流程
2. 邊界情況（空值、超長文本、並發）
3. 錯誤恢復（斷網、API 失敗）
4. **數據庫持久化**（重啟後數據是否還在）
5. **認證有效性**（Token 過期後是否正確跳轉登入頁，而非顯示空界面）
6. **多租戶隔離**（不同用戶登入後，數據是否正確隔離）

---

## 🖥️ 服務器部署信息

### 生產服務器
| 項目 | 值 |
|------|---|
| **主機** | `165.154.210.154` |
| **用戶名** | `ubuntu` |
| **域名** | `tgw.usdt2026.cc` |
| **部署路徑** | `/opt/tg-matrix` |

### SSH 連接命令
```bash
ssh ubuntu@165.154.210.154
```

### 常用運維命令
```bash
# 進入項目目錄
cd /opt/tg-matrix

# 查看運行狀態
sudo docker compose ps

# 重啟 API 容器
sudo docker compose restart api

# 重建並重啟 API（拉取最新代碼後）
sudo git fetch origin && sudo git reset --hard origin/main
sudo docker compose up -d --build api

# 查看 API 日誌
sudo docker compose logs -f --tail=100 api

# 查看所有容器日誌
sudo docker compose logs -f --tail=50
```

**注意**：服務器使用 Docker Compose V2 (`docker compose`)，不是 V1 (`docker-compose`)

---

## 上傳並部署（Git + GitHub Actions）

當用戶說「上傳並部署」「幫我上傳並布署」「提交並部署」等時：
1. 只 stage 源代碼變更（`git add` 後端/前端源文件），**不要**加入 `dist/` 構建產物。
2. 執行 `git commit -m "簡短描述"` 與 `git push origin main`。
3. 推送後 GitHub Actions 會自動執行 Deploy to Production（構建前端、同步服務器、重啟容器）。
4. 可簡短回覆：已提交並推送，Actions 會自動部署。
