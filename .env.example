# TG-Matrix 環境配置示例
# 複製此文件為 .env 並填入實際值

# ===========================================
# 安全配置 (必須修改)
# ===========================================

# 主密鑰 - 用於 Session 加密
SECRET_KEY=your-secret-key-change-this

# JWT 密鑰 - 用於用戶認證令牌
JWT_SECRET=your-jwt-secret-change-this

# 數據加密密鑰 - 用於 Telegram Session 加密
ENCRYPTION_KEY=your-encryption-key-change-this

# ===========================================
# 郵件配置 (可選)
# ===========================================

# 郵件提供商: smtp, sendgrid
EMAIL_PROVIDER=smtp

# SMTP 配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# 發送者信息
EMAIL_FROM=noreply@tg-matrix.com
EMAIL_FROM_NAME=TG-Matrix

# ===========================================
# 數據庫配置
# ===========================================

DATABASE_PATH=/app/data/tgmatrix.db
REDIS_URL=redis://redis:6379/0

# ===========================================
# Telegram 登入 Bot（與網頁版共用）
# ===========================================
# 用於掃碼登入 / 驗證碼登入，須與 BotFather 創建的 Bot 一致
TELEGRAM_BOT_USERNAME=tgzkw_bot
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather

# Bot 確認登入時調用的後端（必須與生成 login token 的後端一致，否則會「Token 不存在」）
# 生產：與對外 API 同機時設為該 API 內網地址，如 http://127.0.0.1:8000
INTERNAL_API_URL=http://127.0.0.1:8000

# ===========================================
# 調試配置
# ===========================================

DEBUG=false
ELECTRON_MODE=false

# ===========================================
# 可選：AI 配置
# ===========================================

# OpenAI
OPENAI_API_KEY=

# Claude
ANTHROPIC_API_KEY=

# 本地 Ollama
OLLAMA_BASE_URL=http://localhost:11434
