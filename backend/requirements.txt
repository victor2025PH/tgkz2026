# TG-Matrix Backend Dependencies
# Python 3.8+ required

# Telegram API
# 使用 2.0.30 避免 is_premium AttributeError bug
# 注意：不要升級到 2.0.106，有媒體發送問題
pyrogram==2.0.30
telethon>=1.34.0  # For QR code login support
opentele>=1.15.1  # TData to Session conversion
TgCrypto>=1.2.5; platform_system != "Windows" or python_version >= "3.8"
# tgcrypto>=1.2.5  # 可选：用于加速加密（需要 C++ 编译器，如果安装失败可以跳过）

# QR Code Generation
qrcode[pil]>=7.4.2
Pillow>=10.0.0

# SOCKS Proxy Support
pysocks>=1.7.1

# Database
aiosqlite>=0.19.0

# Excel file handling
openpyxl>=3.1.2

# Environment variables
python-dotenv>=1.0.0

# Async utilities
aiofiles>=23.2.1

# Cryptography
cryptography>=41.0.0

# Performance monitoring
psutil>=5.9.0
pytest>=7.4.0
pytest-asyncio>=0.21.0

# HTTP client and server
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# Browser automation for API credential scraping (Phase 2)
playwright>=1.40.0

# NumPy (required for vector operations)
numpy>=1.24.0

# ==================== RAG System (Recommended) ====================
# 以下依賴強烈建議安裝，可大幅提升 AI 回覆質量
# 如果安裝失敗，系統會自動降級使用基礎功能

# Vector database (recommended - provides semantic search)
chromadb>=0.4.0

# Neural embeddings (recommended - provides better text understanding)
sentence-transformers>=2.2.0

# 🔧 安裝命令：
# pip install chromadb sentence-transformers
