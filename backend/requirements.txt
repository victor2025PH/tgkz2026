# TG-Matrix Backend Dependencies
# Python 3.8+ required

# Telegram API
# 2025/2026: 必須使用 pyrofork 支持最新 Telegram API Layer
# 原版 pyrogram 2.0.30 的 Layer 143 太舊，會導致 UPDATE_APP_TO_LOGIN 錯誤
pyrofork>=2.3.0
telethon>=1.34.0  # For QR code login support
opentele>=1.15.1  # TData to Session conversion
TgCrypto>=1.2.5; platform_system != "Windows" or python_version >= "3.8"
# tgcrypto>=1.2.5  # 可选：用于加速加密（需要 C++ 编译器，如果安装失败可以跳过）

# QR Code Generation
qrcode[pil]>=7.4.2
Pillow>=10.0.0

# SOCKS Proxy Support
pysocks>=1.7.1

# Database
aiosqlite>=0.19.0

# Excel file handling
openpyxl>=3.1.2

# Environment variables
python-dotenv>=1.0.0

# Async utilities
aiofiles>=23.2.1

# Cryptography and Authentication
cryptography>=41.0.0
argon2-cffi>=23.1.0

# Performance monitoring
psutil>=5.9.0
pytest>=7.4.0
pytest-asyncio>=0.21.0

# HTTP client and server
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# Browser automation for API credential scraping (Phase 2)
playwright>=1.40.0

# NumPy (required for vector operations)
numpy>=1.24.0

# ==================== RAG System (Optional for Production) ====================
# 以下依賴為可選，用於增強 AI 回覆質量
# 在小型 VPS 上建議跳過以減少鏡像大小

# Vector database - using lightweight alternative for production
# chromadb>=0.4.0

# Neural embeddings - DISABLED for production to save disk space (4GB+ PyTorch)
# sentence-transformers>=2.2.0
# 如需啟用，請安裝 CPU 版本：
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# pip install sentence-transformers

# Lightweight text search alternative
Whoosh>=2.7.4
