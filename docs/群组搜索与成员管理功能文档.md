# TG-AI智控王 群組搜索與成員管理功能

## 📦 模塊結構

```
src/group-search/
├── search.types.ts                    # 類型定義
├── search.service.ts                  # 搜索服務
├── member-extraction.service.ts       # 成員提取服務
├── group-search.component.ts          # 搜索主頁面
├── group-detail.component.ts          # 群組詳情頁
├── member-list.component.ts           # 成員列表頁
├── index.ts                           # 模塊導出
│
├── adapters/                          # 搜索源適配器
│   ├── search-adapter.interface.ts    # 適配器接口
│   ├── telegram-adapter.ts            # TG 官方搜索
│   ├── jiso-adapter.ts                # 極搜 Bot
│   ├── tgstat-adapter.ts              # TGStat API
│   ├── local-adapter.ts               # 本地索引庫
│   ├── search-aggregator.ts           # 搜索聚合器
│   └── index.ts
│
└── engines/                           # 核心引擎
    ├── member-extractor.ts            # 成員提取引擎
    ├── batch-operator.ts              # 批量操作引擎
    ├── data-exporter.ts               # 數據導出服務
    └── index.ts

electron/
└── group-search-handler.js            # Electron IPC 處理器
```

---

## 🔍 搜索功能

### 多源搜索架構

| 搜索源 | 說明 | 所需等級 | 特點 |
|:------:|------|:--------:|------|
| ✈️ Telegram 官方 | TG 原生搜索 API | 青銅 | 結果官方、速度快 |
| 🔍 極搜 | 極搜 Bot 對接 | 白銀 | 中文群組豐富 |
| 📊 TGStat | 統計平台 API | 黃金 | 數據詳細、有分析 |
| 💾 本地索引 | IndexedDB 緩存 | 青銅 | 離線可用、即時響應 |

### 搜索配額

| 會員等級 | 每日搜索 | 可用搜索源 |
|:--------:|:--------:|:----------:|
| ⚔️ 青銅 | 5 次 | 1 個 |
| 🥈 白銀 | 20 次 | 2 個 |
| 🥇 黃金 | 50 次 | 3 個 |
| 💎 鑽石 | 200 次 | 4 個 |
| 🌟 星耀 | 500 次 | 4 個 |
| 👑 王者 | 無限 | 4 個 |

### 結果聚合算法

1. **並行搜索**：同時查詢多個搜索源
2. **智能去重**：
   - 優先按 `username` 去重
   - 其次按 `ID` 去重
   - 最後按標題相似度去重
3. **融合評分**：
   ```
   最終分數 = 基礎分數 × 來源權重 + 關鍵詞匹配分 + 成員數加分 + 用戶名加分
   ```

---

## 👥 成員提取

### 提取配額

| 會員等級 | 每日提取額度 |
|:--------:|:-----------:|
| ⚔️ 青銅 | ❌ 不可用 |
| 🥈 白銀 | 100 人 |
| 🥇 黃金 | 500 人 |
| 💎 鑽石 | 2,000 人 |
| 🌟 星耀 | 5,000 人 |
| 👑 王者 | 無限 |

### 提取功能

- ✅ 分批提取（每批 100 人）
- ✅ 實時進度顯示
- ✅ 斷點續傳
- ✅ 可暫停/繼續
- ✅ 自定義篩選條件
- ✅ 防止觸發限制

### 可提取字段

| 字段 | 說明 |
|------|------|
| ID | 用戶唯一標識 |
| 用戶名 | @username |
| 名字/姓氏 | 顯示名稱 |
| 電話 | 手機號（如公開） |
| 頭像 | 頭像圖片 URL |
| 在線狀態 | 在線/最近/上週/上月/很久 |
| 角色 | 創建者/管理員/成員 |
| Premium | 是否付費用戶 |
| 簡介 | 個人介紹 |
| 加入時間 | 加入群組時間 |

---

## 📨 批量操作

### 支持的操作類型

| 操作類型 | 說明 | 所需等級 |
|:--------:|------|:--------:|
| 📨 批量私信 | 向成員發送消息 | 黃金 |
| ➕ 批量邀請 | 邀請成員到其他群 | 黃金 |
| 🏷️ 批量標籤 | 為成員添加標籤 | 白銀 |

### 智能防封策略

```
🛡️ 防封措施：

1. 新帳號保護期
   - 新帳號前 14 天限制每日 10 條
   - 每天增加 5 條配額（預熱模式）

2. 動態延遲
   - 基礎延遲：30-60 秒
   - 隨發送量增加，自動延長間隔
   - 添加隨機波動避免模式識別

3. 智能休息
   - 每發送 10 條後休息 2-5 分鐘
   - 避免連續高頻操作

4. 異常檢測
   - 連續失敗 3 次自動暫停
   - 失敗率超過 20% 自動暫停

5. 消息變體
   - 自動添加不可見字符
   - 增加每條消息的唯一性
```

### 操作限制

| 限制項 | 數值 |
|--------|:----:|
| 每小時上限 | 20 條 |
| 每日上限 | 50 條（可配置） |
| 新帳號每日上限 | 10 條 |
| 失敗重試次數 | 2 次 |

---

## 📥 數據導出

### 支持格式

| 格式 | 說明 | 適用場景 |
|:----:|------|----------|
| 📊 Excel | .xlsx 格式 | 辦公軟件處理 |
| 📄 CSV | 逗號分隔值 | 通用數據交換 |
| 🔧 JSON | 結構化數據 | 程序處理 |
| 📝 TXT | 純文本格式 | 簡單查看 |

### 可導出字段

選擇性導出以下任意字段組合：
- ID、用戶名、名字、姓氏
- 電話、在線狀態、角色
- 是否機器人、Premium 狀態
- 認證狀態、簡介、加入時間

---

## 🎨 UI 預覽

### 搜索頁面

```
┌─────────────────────────────────────────────────────────────────────┐
│ 🔍 群組搜索中心                      [搜索] [歷史] [收藏]   配額顯示 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  搜索關鍵詞 [幣圈 交流群                              ] [🔍 搜索]   │
│                                                                     │
│  搜索源：✓TG官方  ✓極搜  □TGStat  ✓本地        [🎛️ 篩選 (2)]       │
│                                                                     │
│  ┌─ 篩選面板 ────────────────────────────────────────────┐          │
│  │ 類型: [群組 ▼]  語言: [中文 ▼]  人數: [1000+ ▼]  排序: [相關度] │
│  │ ✓ 有用戶名  ✓ 近期活躍                        [清除篩選]        │
│  └──────────────────────────────────────────────────────────────────┘
├─────────────────────────────────────────────────────────────────────┤
│  找到 156 個結果  耗時 234ms                                        │
│                                                                     │
│  ┌────────────────────────────┐  ┌────────────────────────────┐    │
│  │ [頭像] 🔥 幣圈交流群        │  │ [頭像] 💰 區塊鏈投資群      │    │
│  │ @crypto_chat    👥 12.5K   │  │ @blockchain    👥 8.3K     │    │
│  │ 加密貨幣交流群...  [TG官方] │  │ 投資討論區...    [極搜]    │    │
│  │                 ☆ ➕       │  │                 ⭐ ➕       │    │
│  └────────────────────────────┘  └────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 成員列表頁面

```
┌─────────────────────────────────────────────────────────────────────┐
│ ← 返回  幣圈交流群 · 12,500 成員                    額度: 剩餘 450  │
├─────────────────────────────────────────────────────────────────────┤
│ [📥 開始提取]   狀態: [全部 ▼]  ✓有用戶名  ✓排除機器人             │
│                                             [📥 導出] [📨 批量操作] │
├─────────────────────────────────────────────────────────────────────┤
│ ☐  │ 頭像  │ 暱稱        │ 用戶名      │ ID         │ 狀態  │ 標籤 │
│────┼───────┼─────────────┼─────────────┼────────────┼───────┼──────│
│ ☑  │ [👤]  │ 張三        │ @zhangsan   │ 123456789  │ 🟢    │ 管理 │
│ ☑  │ [👤]  │ 李四 ⭐     │ @lisi       │ 234567890  │ 🟡    │      │
│ ☐  │ [👤]  │ 王五        │ -           │ 345678901  │ 🔴    │      │
│ ☑  │ [🤖]  │ BotHelper   │ @helper_bot │ 456789012  │ 🟢    │ BOT  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📝 使用示例

### 1. 基本搜索

```typescript
import { SearchAggregator } from './group-search';

const aggregator = new SearchAggregator();

// 搜索中文群組
const result = await aggregator.search({
  keyword: '幣圈 交流',
  sources: ['telegram', 'jiso', 'local'],
  filters: {
    language: ['zh'],
    minMembers: 1000,
    hasUsername: true
  },
  page: 1,
  limit: 20
});

console.log(`找到 ${result.totalCount} 個群組`);
```

### 2. 成員提取

```typescript
import { MemberExtractor } from './group-search';

const extractor = new MemberExtractor();

// 提取群組成員
const result = await extractor.extract(
  group,
  { hasUsername: true, isNotBot: true },  // 篩選條件
  500,  // 最多提取 500 人
  (progress) => console.log(`進度: ${progress.percent}%`)
);

console.log(`提取了 ${result.members.length} 名成員`);
```

### 3. 批量發送消息

```typescript
import { BatchOperator } from './group-search';

const operator = new BatchOperator();

// 創建批量操作
const operation = operator.createOperation(
  'message',
  selectedMembers,
  {
    messageTemplate: '您好 {name}！歡迎加入我們的社群...',
    delayMin: 45,
    delayMax: 90,
    dailyLimit: 30,
    smartAntiBlock: true
  }
);

// 開始執行
await operator.startOperation(operation, (progress) => {
  console.log(`已發送: ${progress.processed}/${progress.total}`);
});
```

### 4. 導出數據

```typescript
import { DataExporter } from './group-search';

const exporter = new DataExporter();

// 導出為 Excel
const result = await exporter.exportMembers(members, {
  format: 'excel',
  fields: ['id', 'username', 'firstName', 'phone', 'status'],
  includeHeaders: true
});

if (result.success) {
  console.log(`導出成功: ${result.filePath}`);
}
```

---

## ⚠️ 注意事項

1. **遵守 Telegram TOS**
   - 不要用於騷擾或垃圾信息
   - 尊重用戶隱私設置
   - 避免過度頻繁的操作

2. **帳號安全**
   - 新帳號使用預熱模式
   - 遵守操作限制
   - 異常時立即暫停

3. **數據安全**
   - 導出的數據妥善保管
   - 不要分享敏感信息
   - 定期清理不需要的數據

---

## 🔮 後續優化方向

- [ ] 更多搜索源對接（Combot、GroupHelp）
- [ ] AI 驅動的群組推薦
- [ ] 成員行為分析
- [ ] 群組質量評分系統
- [ ] 多帳號輪換支持
- [ ] 雲端同步收藏和歷史
