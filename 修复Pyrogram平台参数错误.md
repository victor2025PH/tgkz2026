# 修复 Pyrogram 平台参数错误

## 🐛 错误信息

```
Client.__init__() got an unexpected keyword argument 'platform'
```

## 🔍 问题原因

Pyrogram 的 `Client` 类**不支持** `platform` 参数。

根据 Pyrogram 2.0.106 的文档，`Client.__init__()` 支持的参数包括：
- ✅ `device_model` - 设备型号
- ✅ `system_version` - 系统版本
- ✅ `app_version` - 应用版本
- ✅ `lang_code` - 语言代码
- ❌ `platform` - **不支持**

## ✅ 修复内容

已从 `backend/telegram_client.py` 中移除了 `platform` 参数：

1. **第一处**（第 166-178 行）：
   - 移除了 `platform=platform or 'android'` 参数

2. **第二处**（第 218-230 行）：
   - 移除了 `platform=device_config.get('platform', 'android')` 参数

## 💡 说明

虽然我们不再传递 `platform` 参数给 Pyrogram Client，但：
- ✅ `device_model` 已经包含了平台信息（如 "iPhone 13" 表示 iOS）
- ✅ `system_version` 也包含了平台信息（如 "iOS 16.6.1"）
- ✅ 这些信息足以让 Telegram 识别设备类型

**平台信息已经通过 `device_model` 和 `system_version` 传递，不需要单独的 `platform` 参数。**

## 🎯 现在请测试

1. **重启应用**
   ```bash
   npm start
   ```

2. **测试登录**
   - 点击"登录"按钮
   - 应该不再出现 `platform` 参数错误
   - 应该能正常发送验证码

---

**修复完成时间：** 2026-01-03  
**状态：** ✅ 已修复

